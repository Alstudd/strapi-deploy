'use strict';

/**
 * upcoming-event controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::upcoming-event.upcoming-event');
